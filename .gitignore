# Geant4 source, build, and install (enormous — not for version control)
geant4/
geant4-build/
geant4-install/

# RADiCAL directory has its own embedded git repo
RADiCAL/

# Compiled build directories and artifacts (platform-specific — never commit)
**/build/
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# ROOT output data files (large binary)
*.root

# macOS metadata
.DS_Store

# Local-only scratch content (not for GitHub)
simulations/RADiCALsimDSB/poster_content.txt

# CMake cache files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
